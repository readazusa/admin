<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ItemVsFile">
    <insert id="save" parameterType="ItemVsFile">
        insert into t_wx_item_vs_file(id,itemId,fileId,createTime,updateTime)
        values(#{id},#{itemId},#{fileId},#{createTime},#{updateTime})
    </insert>

</mapper>
